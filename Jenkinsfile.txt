def ip = "python findingip.py".execute()
pipeline
{
	agent any
	stages{
		stage("running the python script")
		{
			steps{
				script{
						String filecontent=new File("ip_generated.txt").txt
					}
				}
		}
	}
}